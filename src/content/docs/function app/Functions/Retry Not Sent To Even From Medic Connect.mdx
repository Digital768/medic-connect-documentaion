---
title: Retry Not Sent To Even From Medic Connect
description: A guide in my new Starlight docs site.
---

import { Code } from "@astrojs/starlight/components";

**Type:** Timer trigger.

**Auth Level:** -

**Description:** Sends to even the requests that their sending have failed

<Code
  title="Retry Not Sent To Even From Medic Connect"
  lang="python"
  
  code={`
    @app.function_name(name="retry_not_sent_to_even_from_medic_connect")
    @app.schedule(schedule="*/5 * * * *", arg_name="timer", run_on_startup=False)
    def retry_not_sent_to_even_from_medic_connect(timer: func.TimerRequest) -> None:
    `}
/>

## Expected input

---

## Expected return

---

<Code
  title="Valid Response"
  lang="python"
  
  code={`
      if response.ok:
        Functions.utils_functions.logger.info("Api call successful")
        return func.HttpResponse(f"Success", status_code=200)
      else:
        Functions.utils_functions.logger.info(
          f"Api call failed with status code {response.status_code}"
        )
        return func.HttpResponse(
          f"Error: Api call failed with status code {response.status_code}",
          status_code=response.status_code,
      )
    `}
/>

### Valid response

###### Body

returns success

###### Status

200 OK

### Exception response

###### Body

returns failed

###### Status

response status code

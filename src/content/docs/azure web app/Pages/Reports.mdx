---
title: Reports
---
import { Image } from 'astro:assets';
import reportspage from '../../../../assets/reports.png'; // Image is 1600x900
import { FileTree } from "@astrojs/starlight/components";

<FileTree>
- routes
    - **_user.reports.tsx**
    - ...
    - **admin.reports.tsx**
</FileTree>

<Image src={reportspage} alt={"Reports Page"}/>

### Page Features
- Display reports of requests by status - approved, rejected and error.
- Filter the reports by their dates (also hospital and the user that created the request if the user in an admin).


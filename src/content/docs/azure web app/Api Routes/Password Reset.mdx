---
title: Password Reset
description: A guide in my new Starlight docs site.
---

import { Code, FileTree } from "@astrojs/starlight/components";

**Description:** This api resets the user's password.

<FileTree>
- routes
  - **api.password-reset.ts**
</FileTree>

## Expected input

---

request type Request contains formData of the user's id, his new password and verification of it

<Code
  title="Password Change Api Request Json"
  lang="json"
  code={`
    {
         "userId": number,
        "newPassword": string,
        "newPasswordRepeat": string,
    }
    `}
/>


## Expected return

---


### Valid response

###### Body
<Code
  title="Password Reset Api Valid Response"
  lang="python"
  
  code={`
    return json({ success: { res: 'סיסמה עודכנה' } }, { status: 200 });
`}

/>

###### Status

200 OK

### Exception response

###### Body

<Code
  title="Password Reset Api Exception Response"
  lang="python"
  
  code={`
    const res = await changePassword(data.userId, hashedNewPassword);
    if (!res)
      return json(
        { errors: { root: { message: 'עדכון הסיסמא נכשל' } } },
        { status: 500 },
      );
`}

/>
###### Status

500 internal server error